import React from "react";

const UserContext = React.createContext()   // initially UserContext is null
// it is not mandatory that file name and context name should be same

export default UserContext;